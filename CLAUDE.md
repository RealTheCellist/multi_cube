# Poly Puzzle — 작업 관행

## Sprint 결과 보고
각 작업지시서(Sprint) 구현을 마치고 커밋/푸시까지 끝나면, 사용자가 별도로
요청하지 않아도 그 결과를 "conclusion" 문서로 자동 작성해 전달한다.

- 형식: Markdown 파일만 생성한다 (HTML 버전은 만들지 않는다).
- 내용: 무엇을 했는지, 실제 수치/벤치마크 결과, 성공/실패 기준 판정,
  결론(통합 여부 또는 다음 단계 제안)을 포함한다.
- 전달: `SendUserFile`로 전달한다.
- 게시: 같은 MD 파일을 `Artifact`로도 게시해 사용자가 채팅에서 바로
  내용을 볼 수 있게 한다 (파일 그대로 게시 — 별도 HTML로 변환하지 않는다).
- 채팅 응답에는 핵심 요약만 짧게 남기고, 전체 서술은 MD 파일에 담는다.
